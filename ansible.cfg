[defaults]
remote_user = nexus
inventory = inventory.ini
host_key_checking = false
roles_path = /home/nexus/github/Ansible/roles

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
